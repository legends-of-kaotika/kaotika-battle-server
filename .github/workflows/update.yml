name: PM2 Update
on:
  push:
    branches:
      - main
      - KBS-50

jobs:
  pm2-update:
    name: Update PM2 Server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Update and restart PM2
        run: |
            cd /home/daw1/Documentos/Kaotika Battle Server/kaotika-battle-server && git config pull.rebase false && git fetch && git pull origin main && npm install && pm2 reload ecosystem.config.js --watch -i max && 